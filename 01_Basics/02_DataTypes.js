"use strict"; //using JS code's newer version

//alert(10-1); // We are using nodejs, not web browser so it won't work here.

let userName = "sachin";
let age = 18;
let isLoggedIn = false;

console.log(typeof "Sachin");